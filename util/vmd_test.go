package util

import (
    "testing"
)

func TestVMD_Load(t *testing.T) {
    //v := new(VMD)
    //dir, _ := os.Getwd()
    //err := v.Load(path.Join(path.Join(dir, "test_data"), "kishimen.vmd"))
    //if err != nil{
    //    t.Error(err)
    //}
    //if v.ModelName == "初音ミク(メタル服)"{
    //    log.Println(v.ModelName)
    //}else{
    //    t.Error(v.ModelName, "is not 初音ミク(メタル服)")
    //}
}
